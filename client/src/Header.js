import React from 'react';

function Header() {
    return (
        <header style={{textAlign: 'center', marginTop: '2rem'}}>
            <h1 style={{fontSize: '3rem'}}>Math Resources</h1>
        </header>
    );
}

export default Header;
